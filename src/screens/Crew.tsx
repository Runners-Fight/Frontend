import {View, Text} from 'react-native';

const CrewScreen = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Crew Screen</Text>
    </View>
  );
};

export default CrewScreen;
